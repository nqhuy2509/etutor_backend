export enum StatusUser {
    pending,
    verified,
    active,
    suspend,
}

export enum LoginType {
    local,
    google,
    facebook,
}

export enum TypeUser {
    parent = 'parent',
    teacher = 'teacher',
    student = 'student',
}
